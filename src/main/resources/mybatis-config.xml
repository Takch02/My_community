<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <!-- 타입 별칭 설정 -->
    <typeAliases>
        <typeAlias alias="Post" type="clonecoding.velog_clone.dto.Post"/>
        <typeAlias alias="User" type="clonecoding.velog_clone.dto.User"/>
    </typeAliases>

    <!-- 매퍼 파일 위치 -->
    <mappers>
        <mapper resource="mapper/PostMapper.xml"/>
        <mapper resource="mapper/UserMapper.xml"/>
        <!-- 다른 매퍼 파일이 있다면 추가 -->
        <!-- <mapper resource="mapper/UserMapper.xml"/> -->
    </mappers>
</configuration>